import 'package:flutter/material.dart';

class PagevScreen extends StatelessWidget {
  const PagevScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PagevScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}